#ifndef GP_MANIFOLD_NLOPT_OPTIMIZER
#define GP_MANIFOLD_NLOPT_OPTIMIZER

namespace gp_manifold {
    namespace optimization {
        class NloptOptimizer {
        public:
            NloptOptimizer()
            {
            }

            ~NloptOptimizer()
            {
            }

        protected:
        };
    } // namespace optimization
} // namespace gp_manifold

#endif // GP_MANIFOLD_NLOPT_OPTIMIZER